package com.integration.execution.contract.model;

import com.integration.execution.contract.model.enums.CredentialAuthType;

public interface AuthCredential {
    CredentialAuthType getAuthType();
}
